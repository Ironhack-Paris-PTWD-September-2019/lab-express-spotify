<div class="container">
{{#each albumsitems}}
<div class="artist-container">
    {{#each this.images}}
        {{#if @first}}
            <img src={{{this.url}}}>
        {{/if}}
    {{/each}}
    
    <h3>{{{this.name}}}</h3>
    <a href="/tracks/{{this.id}}">View Tracks</a>
   
    </div>
{{/each}}
</div>